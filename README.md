# ioniccli-meteor-tabs
Ionic2 tabs starter app using IonicCLI (Webpack)

`npm install`

If not already installed globally: `npm install -g meteor-client-bundler`

Add symlinks in `api/` to `node_modules` and `package.json`
